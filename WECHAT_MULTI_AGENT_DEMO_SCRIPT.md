# 公众号多 Agent 演示脚本

## 1. 文档定位

这份文档只服务一个目的：把“公众号多 Agent 工作台”的演示路径收敛成可重复执行的脚本，而不是每次现场临时发挥。

适用场景：

- 答辩演示
- 老师现场抽查
- 新窗口接手后的快速回放

## 2. 演示前准备

建议在正式演示前确认以下前提：

1. 前端和后端已经启动，浏览器可正常打开工作台。
2. 当前账号已登录，避免现场先卡在登录守卫。
3. 本地至少存在一个可展示的公众号账号和历史文章。
4. 如需展示评测趋势，当前机器已存在官方 RAGAS 快照，或至少已跑过一次评测。

## 3. 推荐主链路

推荐入口：公众号多 Agent 工作台。

推荐讲解顺序：

1. 先看左侧“多 Agent 主视图”。
2. 说明当前页面只保留输入、阶段状态、必要时才展开的人工工具。
3. 输入一条自然语言指令，演示从入口判断到采集、治理、评测的完整闭环。
4. 用“判断质量统计”“Handoff 契约诊断”“评测趋势”证明系统不是黑箱。

## 4. 成功样例

### 4.1 推荐输入

可选示例：

- 抓取这个公众号链接并完成治理和评测：https://mp.weixin.qq.com/s/示例链接
- 继续处理这个公众号链接，抓取后直接做治理检查和评测：https://mp.weixin.qq.com/s/示例链接

### 4.2 预期页面信号

1. 入口判断诊断会显示本轮由 LLM 或规则完成判断。
2. 知识采集 Agent 卡片会出现“已完成采集 Agent”或等价状态。
3. Handoff 契约诊断会显示“采集 Agent -> 治理 Agent”已完成或等待下一跳。
4. 治理 Agent 完成后，评测 Agent 卡片会显示质量分、覆盖率和 RAGAS 摘要。
5. 评测趋势卡会新增一条最近记录。

### 4.3 推荐讲解词

可以直接按下面口径讲：

“这条指令先由入口判断层解析，再进入知识采集 Agent。只要采集阶段形成了新的知识资产，系统就会自动尝试 handoff 给治理 Agent；治理报告就绪后，再自动 handoff 给评测 Agent。左侧这几张卡片都是后端返回的统一事实源，不是前端自己猜的状态。”

## 5. 零新增样例

### 5.1 推荐输入

- 重新处理这个之前抓过的链接：https://mp.weixin.qq.com/s/历史已抓取链接

### 5.2 预期页面信号

1. 顶部结果条会明确说明“本轮未新增知识资产”。
2. 失败原因诊断会显示“重复链接”或“时间窗过滤”等结构化分类。
3. Handoff 契约诊断会直接显示没有形成下一跳输入，而不是笼统写成失败。

### 5.3 推荐讲解词

“这里不是系统报错，而是系统确认这条链接已经处理过，所以没有必要重复进入治理和评测。现在页面会把这种情况明确标为‘重复链接’或‘时间窗过滤’，避免把‘零新增’误读成‘抓取失败’。”

## 6. 失败降级样例

### 6.1 推荐输入

- 抓取这个公众号文章，如果直抓不行就尝试给出原因：https://mp.weixin.qq.com/s/可能受限链接

### 6.2 预期页面信号

1. 如果页面被微信验证或访问保护拦住，失败原因诊断会显示“页面访问受限”。
2. 如果当前能力不支持，页面会停在“能力边界拦截”。
3. 如果采集成功但治理或评测失败，结构化失败原因会明确显示停在哪一跳。

### 6.3 推荐讲解词

“这类失败现在不会再混成一句模糊反馈。页面会明确告诉我，问题是出在入口判断、采集、治理还是评测，以及这一跳属于重复、页面受限、时间窗过滤还是实际执行失败。现场如果这条链路不稳定，我会切到已经准备好的备选链路，而不是当场排大故障。”

## 7. 备选演示动作

如果主链路现场不稳定，建议优先切换到下面两种备选动作：

1. 直接展示最近一次成功运行留下的编排卡片、评测趋势和判断统计。
2. 使用一个已知重复链接，演示“零新增不等于失败”的产品口径。

## 8. 演示后最低留痕

演示结束后至少补一条：

1. 本次使用了哪条输入。
2. 页面最终停在采集、治理还是评测。
3. 是否出现了零新增、页面受限或真实失败。
4. 如果现场口径与文档不一致，需要同步更新接力文档或里程碑文档。